#include<iostream>
#include<cstdlib>
#include<ctime>
using namespace std;
/* 
int main(){
    srand(time(NULL));
    int r[10], firstbit=0, secondbit=0, range=8, range1=16;
    for(int k=0;k<1;k++){
    do{firstbit=(rand()-RAND_MAX/2)%(range);
        secondbit=(rand()-RAND_MAX/2)%(range);
        cout << firstbit << endl << secondbit << endl;}
    while(firstbit==secondbit);
    cout << firstbit << endl;
    }
   // cout << rand() << endl;
}
*/
